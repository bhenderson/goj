goj

TODO:

* sort map keys
* diff two jsons
* proper go docs
* inspect performance
* proper cmd usage
* support script expression
  * '**.book(@.length)', 'store(@.keys)', 'store.book[*](@.keys)'
  * '**.book(length)', 'store(keys)', 'store.book[*](keys)'

